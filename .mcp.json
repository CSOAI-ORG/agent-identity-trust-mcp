{
  "name": "agent-identity-trust-mcp",
  "description": "> **By [MEOK AI Labs](https://meok.ai)** \u2014 Sovereign AI tools for everyone.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "register_agent_identity",
      "description": "Register an agent with a decentralized identifier (DID) and capability attestation.",
      "parameters": {
        "type": "object",
        "properties": {
          "agent_name": {
            "type": "string"
          },
          "capabilities": {
            "type": "string"
          },
          "organization": {
            "type": "string"
          }
        },
        "required": [
          "agent_name",
          "capabilities"
        ]
      }
    },
    {
      "name": "issue_credential",
      "description": "Issue a verifiable credential to an agent (compliance cert, capability proof, etc.).",
      "parameters": {
        "type": "object",
        "properties": {
          "agent_did": {
            "type": "string"
          },
          "credential_type": {
            "type": "string"
          },
          "claims": {
            "type": "string"
          }
        },
        "required": [
          "agent_did",
          "credential_type",
          "claims"
        ]
      }
    },
    {
      "name": "verify_credential",
      "description": "Verify a credential is valid and not expired.",
      "parameters": {
        "type": "object",
        "properties": {
          "credential_id": {
            "type": "string"
          }
        },
        "required": [
          "credential_id"
        ]
      }
    },
    {
      "name": "get_agent_reputation",
      "description": "Get an agent's reputation score and history.",
      "parameters": {
        "type": "object",
        "properties": {
          "agent_did": {
            "type": "string"
          }
        },
        "required": [
          "agent_did"
        ]
      }
    },
    {
      "name": "list_registered_agents",
      "description": "List all registered agents, optionally filtered by organization.",
      "parameters": {
        "type": "object",
        "properties": {
          "organization": {
            "type": "string"
          }
        },
        "required": []
      }
    }
  ]
}
